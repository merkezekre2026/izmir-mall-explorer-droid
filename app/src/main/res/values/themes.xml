<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose kendi temasını kodda yönetir, XML tarafı sadece Activity sarmalayıcısı -->
    <style name="Theme.IzmirAVMMap" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
